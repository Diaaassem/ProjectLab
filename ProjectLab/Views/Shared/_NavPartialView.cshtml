@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@

<!-- Navigation-->
<nav class="navbar navbar-light bg-light static-top">
    <div class="container">
        <a class="navbar-brand" href="#!">Start Bootstrap</a>
        <div>
            <a asp-controller="Student" asp-action="getAll" class="btn btn-primary me-2">Students</a>
            <a asp-controller="Instructor" asp-action="getAll" class="btn btn-primary me-2">Instructors</a>
            <a asp-controller="Department" asp-action="getAll" class="btn btn-primary me-2">Departments</a>
            <a asp-controller="Course" asp-action="getAll" class="btn btn-primary me-2">Courses</a>
            
            @if (User?.Identity?.IsAuthenticated == true)
            {
                <span class="text-success me-2">Logged in as: @User.Identity.Name</span>
                <form asp-controller="Account" asp-action="Logout" method="post" style="display:inline;">
                    <button type="submit" class="btn btn-danger">Logout</button>
                </form>
            }
            else
            {
                <a asp-controller="Account" asp-action="Login" class="btn btn-success">Login</a>
            }
        </div>
    </div>
</nav>
